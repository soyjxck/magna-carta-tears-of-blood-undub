roms/
work/
build/
*.iso
*.SFD
*.AFS
*.adx
*.m1v
*.elf
*.bin
__pycache__/
*.pyc
.DS_Store
.venv/
venv/
.claude/

# Local-only archives — kept on disk for reference but never published.
# The D-build trail and per-extension SHIP.AFS analyses lived here during
# investigation; the canonical pipeline lives in lib/{afs,iso,ship,cutscenes,ffmpeg}.py.
lib/experiments/
docs/

# Translation catalogs are derived from a user's own ISOs by
# `patch.py translate-extract`.
translations/
