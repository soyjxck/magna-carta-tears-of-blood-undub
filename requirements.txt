sfd-muxer
cri-afs

# External tools needed on PATH (install via brew/apt):
#   ffmpeg (with libass) — auto-built into work/tools/ if not present
#   xdelta3              — generate distributable patch
#   7z                   — ISO extraction
